📚 Aula 8: Utilizando **Módulos** em Python

Nesta aula, o foco é entender como utilizar **módulos** para adicionar novas funcionalidades aos nossos programas em Python. 🚀  

---

🔹 1. O que são Módulos?

Pense nos módulos como **"bibliotecas" de código** que já vêm com funções prontas.  
O Python não inclui todos os comandos possíveis por padrão para se manter leve e rápido.  

👉 Quando precisamos de uma funcionalidade específica (como cálculos matemáticos avançados ou geração de números aleatórios), nós **importamos o módulo correspondente**.

---

🔹 2. Como Importar e Usar Módulos

Existem duas formas principais de importar módulos:

✅ Importação Geral (`import`)
Traz **todas as funcionalidades** do módulo de uma vez.  
Para usar uma função, você precisa **especificar o nome do módulo primeiro**.

Exemplo:
```python
import math

raiz = math.sqrt(25)
print(raiz)  # 5.0
